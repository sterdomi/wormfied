# Firebase-Webkonfiguration für die globale Bestenliste (src/services/firebase.ts).
# Werte aus der Firebase Console → Projekteinstellungen → Deine Apps → SDK-Konfiguration.
# Lokal `.env` anlegen (wird nicht committet) und ausfüllen.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
